# SORA / GameTarget プロジェクトメモ

作業を始める前に、以下を必ず読んでください:

- 理念.md (公表用: プロジェクトの理念。何のために作っているか)
- 仕様検討メモ.md (SORA本体の仕様。分量が多いので目次的に拾う)
- GameTarget風モデル検討.md (開発途中版の風モデル)
- tmp/検討履歴.md (非公開: これまでの検討・決定の時系列ログ。**全体像を掴むならまずこれ**)
- tmp/理念-内省メモ.md (非公開: 理念との向き合い方についての内省)
- tmp/次回検討項目.md (非公開: 全体見取り図)
- tmp/地球の健康診断構想.md (非公開: 地震モニターとの連携構想)
- tmp/Git運用メモ.md (非公開: push手順。**現行方針が変わっているので必読**)

## 現在の状況(2026-08-27時点)

- **リポジトリ運用**: SORA(origin)は `2d1bd0a` で**凍結**。以降は **GameTarget(gametarget)のみ**へ push する
- **開発の焦点**: `?dev=1`(devMode)の風モデル。気圧配置(H・L)モデル + 日周期変動 + 実データ連携
- **実データ連携**: Open-Meteo から18項目中11項目を実装済み(第1〜5段階)。
  **既定の飛行挙動は一度も変えていない** — すべて「表示のみ」か「ボタンで明示的に適用したときだけ効く」形
- **街並み表示(PLATEAU)**: 2026-08-27に第1段階まで完了。`?city=1`(箱のみ)/ `?city=2`(写真つき)。
  `prototype/plateau.js` が読み込みを担う。これも**表示のみ**で飛行挙動は不変。
  入口は「本格モード」オーバーレイのボタン。検証ページは `experiments/plateau/`。
  **注意: 既定5エリア中2つが未整備、佐賀は会場側(佐賀市)が未整備**
- **チェイスカー(地上クルー)**: 2026-08-27に**仕様決定**(`tmp/チェイスカー仕様メモ.md`)。
  道路は**地理院ベクトルタイル**の `road` レイヤ(OSMは不採用)。第0段階の実データ検証で
  接続グラフを組めることを確認済み(`experiments/chasecar/probe-graph.mjs`)。
  走行モデルは自動追尾+最小限の指示2種。**ズームは全域 z14 の単一ズーム**
  (z16 の「余分な」線は道路縁で、走行グラフに混ぜてはいけない。z14 はフィルタ済み)。
  ただし全域一括ではなく**周囲だけ随時読む**。
  **第1段階(描画のみ)・第2段階(走行)完了**: `prototype/road.js` / `prototype/chasecar.js` / `?dev=1&road=1`。
  A*(コストは所要時間)を5秒おきに引き直し、飛行中に道路タイルを足していく。
  30分の追尾で気球との距離は佐賀467m・渡良瀬609m(上士幌は道が疎で2,376m)。
  これも**表示のみ**で飛行挙動は不変
- **第二チェイスカー(2号車)**: 2026-08-28に**仕様決定**(`tmp/チェイスカー仕様メモ.md`)。
  離陸地点から**ターゲットへ向かい、約100m手前で待機**し、**ターゲット付近の地上風を報告**する。
  「予想着陸点へ先回り」は**採らなかった**(根拠のない予想を出さないため)。
  視点は `V` の循環に「2号車から気球を見る」1つだけ追加(合計5つ)。
  到着後は経路探索も移動計算も止まるので**1号車より軽い**。
  **報告は第3段階そのものなので、2号車を先にやって1号車へ展開する**。
  実装前の確認事項はタイル枚数(上限48枚)の1点。
  他に H・Lデータの保存方式、追加タスク種別(PDG・FIN等)、実機スマホ確認。
  実データの残り7項目はいずれも補助的で優先度は高くない

## 進め方の原則(この案件で繰り返し効いたこと)

- **実装前に実データで確かめる**。CAPEの日変化、日射量の予測力、ガストの閾値、日周期係数の
  ばらつきなど、思い込みが実際に何度も外れている
- **根拠のない数値を基準のように見せない**。明確な基準がない項目は合否を判定せず注意喚起にとどめる
  (理念「誤情報で混乱を生まない」)
- **既定の挙動を壊さない**。物理計算に関わる変更は、既定値を変えずボタンで任意適用にする
- 変更後は4つの入口(既定 / `?setup=1` / `?a=` / `?dev=1`)で回帰確認する

## ローカル動作確認

```
python -m http.server 8000 --directory c:\Claude\game
```
→ http://localhost:8000/prototype/?dev=1

**モジュール間で export を足したときは Ctrl+Shift+R(強制再読み込み)**。
ブラウザが古い `.js` を掴むと `does not provide an export named ...` で
**モジュール全体が評価されず、画面が真っ暗になる**(2026-08-28 に実際に遭遇)。
通常の再読み込みでは直らない。
