# 熱気球フライトゲーム 仕様検討メモ

国土地理院の地形データを読み込んで遊ぶゲームの仕様検討メモ。
仕様は確定済み。**プロトタイプ実装に着手済み**(`prototype/` フォルダ)。

本プロジェクトには別途理念ファイルあり(非公開)。

## 名称(2026-07-17決定)

- **hot-air-balloon1**: 既定フロー簡略化・住所検索・モバイル対応より前の、変更前のプログラム名
  (エリア選択→ブリーフィングを毎回通る、JDG競技に詳しい人向けの作り)
- **SORA**: 変更後の現行プログラム名。**熱気球の競技を知らない人のためのプログラム**という位置づけ。
  初見の人でもすぐ気球を飛ばせて、熱気球のフライトを楽しめることをコンセプトとする
  (本格的にJDG競技を遊びたい人向けには `?setup=1` で hot-air-balloon1 相当のフルブリーフィングに入れる)
- コード上は `<title>` を「SORA - 熱気球フライトシミュレーター」に変更し、読み込み画面に
  ロゴ「SORA」とタグライン「熱気球のフライトを、誰でもすぐに。」を表示するようにした
- フォルダ構成は変更していない(`prototype/` のまま)。`hot-air-balloon1`/`SORA` はプログラムの
  バージョン名・コンセプト名であり、フォルダを分ける対応はしていない
  (ローカルでの旧バージョン保存が必要な場合は、ユーザー側でフォルダを複製・リネームする形を想定)
- **リポジトリ**: GitHub上は `SORA` リポジトリ(`origin`)と `GameTarget` リポジトリ(2つ目のリモート
  `gametarget`)の両方に、同じローカルフォルダ(`c:\Claude\game`)から push している。
  `GameTarget` は新機能(下記の地上風の揺らぎ等)を検討・開発するための作業用リポジトリという位置づけ。
  push手順の詳細(SORA/GameTargetの使い分け・トラブル対応)は別途 `Git運用メモ.md` を参照

## プロトタイプ現況(2026-07-17時点)

- 場所: `prototype/index.html` + `main.js` + `terrain.js`(ブラウザ+Three.js、ビルド不要)
- 起動: `python -m http.server 8000 --directory c:\Claude\game` → http://localhost:8000/prototype/
- 実装済み: 佐賀・嘉瀬川周辺20km四方の地形(DEM z13+写真z14)、バーナー(Space長押し)/リップライン(R長押し)、
  高度別風レイヤー(パイバルプリセット固定)、計器HUD、パイバル表、時間加速×1〜×8、視点切替(V)、燃料、出典表記
- 実装済み(JDG): マーカー投下(M、1本)。重力+空気抵抗(終端速度10m/s)で落下し落下中も風に流される。
  ターゲット(オレンジX+白リング、エリア中央)、着地点→ターゲットの距離をリザルト表示、
  自己ベストをlocalStorageに保存、HUDにターゲット距離/方位を常時表示
- 実装済み(競技の流れ): タスクブリーフィング画面(タスクシート+パイバル表+標準地図)。
  地図はズーム(ホイール、z11〜z17タイル切替)・パン(ドラッグ)対応のスリッピーマップ。
  地図クリックで離陸地点を自由選択(個別離陸)→離陸ボタンで飛行開始。
  制限時間30:00(ゲーム内時間)をHUDでカウントダウンし、時間内に投下できなければ現在地点計測にフォールバック
- 実装済み(風): ブリーフィングでパイバルを手入力可能(行の追加削除・プリセット4種・数値編集)。
  風テーブルは `?w=ft,dir,kt;…` 形式でURLに載り、「条件URLをコピー」ボタン(ブリーフィング/リザルト)で共有可能。
  風向補間は最短角度経路(350°→020°は北回り)に対応
- 実装済み(地形LOD): 気球の近く(3km以内)の地形タイルを裏でz16写真(約2m/px、従来の4倍精細)に
  段階アップグレード(1.5秒に1枚、上限12枚)。離陸地点とターゲットのタイルは離陸時に即時高解像度化。
  デバッグ用 `?autostart=1` でブリーフィングを飛ばして即離陸可能(自動テスト用、後述の既定フロー変更で現在は非推奨・下記参照)
- 実装済み(地形LOD第2段): 対地高度1000m未満のとき、気球直下の1タイルのみz17(約1m/px、4096px)へ
  さらにアップグレード。直下タイルが変わると前のタイルはz16に戻し、z17はメモリ上常に1枚だけ保持(実機確認済み)
- 実装済み(エリア選択): 起動時に日本全図のスリッピーマップでエリア中心をクリック選択
  (橙枠=20km四方プレビュー)。プリセット5件(佐賀・嘉瀬川/渡良瀬遊水地/佐久・千曲川/一関・平泉/上士幌)。
  エリアはURLの `?a=lon,lat` に載り、風(`&w=`)とセットで共有可能。`?a=`付きURLで開くと選択画面をスキップ。
  ターゲットは選択エリアの中央。海上など標高タイル欠損は標高0として処理(クラッシュしない)
  ※`?a=`直行→ブリーフィングの実機確認は未了(ヘッドレス不安定のため)。選択画面表示は確認済み
- 実装済み(一人称視点): ゴンドラ視点を作り直し。目の位置を気球に固定し、マウスドラッグで視線方向(ヨー・ピッチ、
  ピッチは±85°に制限)だけを回す方式に変更(従来は視線先の点を中心に軌道し目の位置がずれる挙動だった)。
  Vキーで視点切替時、直前の視線方向を引き継いでゴンドラ視点に入る/三人称視点に戻る際は見ていた向きの後方に回り込む
- 実装済み(ゴンドラ造形・2026-07-10): **四角い編みかご**構成で作り直し(PC版・モバイル版共通)。
  4面の壁(Canvas生成の編み目テクスチャ、内側からも見えるDoubleSide)+床+革張りリム(壁上面を覆う太さ)+
  四隅の革巻き風支柱+バーナー本体。一人称で見下ろすと「かごの中にいる」内観になることを
  スクリーンショット2アングル(-75°/-35°)で確認済み。三人称外観は実機確認待ち。
  検証用デバッグパラメータ `&pitch=-40&yaw=0`(fpv=1と併用、開始時の視線方向指定)を追加(モバイル版のみ)
- 実装済み(サウンド・2026-07-10): バーナー音(ノイズ600Hzバンドパス)・リップライン排気音(2600Hz)・
  風のアンビエント(風速連動、380Hzローパス)をWeb Audio合成で再実装。前回鳴らなかった原因は
  AudioContextがsuspendedのままresume()していなかったことと推定し、ユーザー操作(keydown/pointerdown)の
  たびにresumeする対処を実装。PC版・モバイル版共通。音量調整は updateSounds() 内の数値で可能
- 見送りのまま: バーナー使用中のカメラ微小シェイク、炎の毎フレームちらつき(ユーザー判断で不要)
- 実装済み(一人称の作り込み第2弾・2026-07-10、PC版・モバイル版共通):
  - 球皮内外にゴア(16枚の縦パネル)模様。内面は専用メッシュ(BackSide・自発光風の明るい配色)で
    「日光が透ける布」を表現。見上げるとゴアが放射状に集まる実機らしい見え方(スクリーンショット確認済み)
  - バーナー炎を二重コーン化(外オレンジ+芯黄、openEnded)し、基部をバーナー上端(y2.4)へ。
    点火中は球皮内面が滑らかに暖色へ明るくなる(ランダムなちらつきは不使用)
  - リップラインのロープ(赤、ゴンドラ→球皮の口)。R/排気ボタンを引いている間はロープが引き下がる
  - 一人称の立ち位置をゴンドラ中心から0.45m横へ(実機のパイロット位置。真上の炎が正しく見える)
  - マーカーに常時カメラを向く発光スプライト+リボン拡大。落下中はHUDに「落下中 ○○m」表示
  - デバッグ: `&burn=1`(autostart併用)で開始時からバーナー点火(モバイル版のみ)
- 実装済み(スカート・2026-07-10): 球皮の口からバーナー上方へ絞る開放円錐(上端r3.0/下端r1.5/高さ3.2、
  明るめの赤0xb52a2a。ユーザー指示で当初案より短く・明るく調整)。
  上端半径を球皮のy=7.0断面半径と一致させて接続し、継ぎ目の浮きを解消(過去2回の失敗原因)。
  三人称の気球らしいシルエットと、ゴンドラ視点の見上げ(内側もDoubleSideで表示)をスクリーンショット確認済み。
  PC版・モバイル版共通
- デバッグ用 `?fpv=1`(`?autostart=1`と併用)でゴンドラ視点のまま開始可能
- 実装済み(コンパス): 画面下部中央に円形コンパス。カメラ(画面正面)の磁方位を数値+回転文字盤で表示し、
  N(赤)/E/S/Wの方位文字と30°刻みの目盛り、ターゲット方向のオレンジ印付き。一人称・三人称どちらでも機能
- 実装済み(既定フローの簡略化・2026-07-17): 熱気球競技を知らない人向けに、起動時の既定動作を変更。
  - `?setup=1` なし・`?a=` なしの**初回起動**: エリア選択・ブリーフィング画面を出さず、既定エリア(佐賀・嘉瀬川)+
    既定パイバルのまま、風上約3km地点(風から自動算出)から即フライト開始
  - `?setup=1` あり: 従来通りのフルJDGブリーフィング(タスクシート・パイバル編集・地図での離陸地点選択)
  - `?a=lon,lat` あり(住所検索・共有URL経由): タスクシート/パイバル編集は省くが、
    「地図で離陸地点を選択してください」という簡略版ブリーフィング(離陸地点選択の地図のみ)を表示。
    風から自動算出した地点をあらかじめ選択済みにしておき、そのまま離陸してもよいし選び直してもよい
  - 旧デバッグ用 `?autostart=1` は上記の初回起動フローと同義になったため実質的に非推奨(残置はしている)
- 実装済み(住所検索でのエリア移動・2026-07-17): 画面上部中央に住所検索欄を常時表示(国土地理院 Address Search API使用)。
  - 住所・地名を入力すると候補一覧を表示。選択すると全画面の地図オーバーレイ(「位置を確認」)が開き、
    候補地点を中心に地理院標準地図を表示。ズーム初期値をz13まで寄せ、20km飛行エリア枠(橙色)が大きく見えるように調整
    (ホイール/ピンチでz16まで追加ズーム可)。クリック/タップで位置を微調整し、「この場所へ移動」で確定
  - 番地・建物名など詳細な住所は、GSI側のデータ精度により市区町村など大まかな場所までしかヒットしないことがある
    (仕様上の制約。地図での目視確認・微調整で対応する設計にした)
  - 確定すると `?a=lon,lat` を付けて再読み込みし、上記の「地図で離陸地点を選択してください」フローに合流する
  - 日本国外の座標がヒットした場合は弾いてエラー表示
- 実装済み(使い方リンク・2026-07-17): ヘルプパネルに「使い方」リンクを追加。クリックすると全画面オーバーレイが開き、
  「動画」欄からInstagram / YouTubeの使い方動画へ遷移できる
- 未実装(次の候補): 共通ローンチサイト指定のタスク

## モバイル対応(2026-07-17更新: `mobile/` フォルダ案から方針転換)

当初(2026-07-09)は `prototype/`(PC版)を凍結し、スマホ対応版を別フォルダ `mobile/` に分離する方針だったが、
**プロジェクト・フォルダを分けずに1つのコードベースで保守する方針に変更**(2026-07-17)。
`prototype/` の `index.html` / `main.js` に直接タッチ対応を統合済み。PC・スマホ両対応の単一コードベースになっている。

- **タッチ操作ボタン**(画面右下、フライト画面に常時表示): 🔥バーナー(長押しで噴射)/ RIPリップライン(長押しで排気)/
  📍マーカー投下(タップ)/ 👁視点切替(タップ)/ ×1時間加速切替(タップで×1→×2→×4→×8巡回)/ Pパイバル表示切替(タップ)。
  ポインタイベント(pointerdown/up/cancel/leave)で長押し判定、コンテキストメニュー抑止
- **タッチ検知**: `matchMedia('(hover: none) and (pointer: coarse)')` でタッチ端末を判定し、
  該当時のみタッチボタンを表示・キーボードショートカット説明を非表示・パイバル表を初期非表示にする
  (キーボード操作もそのまま併用可なので、タッチ対応PCでもキーボードがあれば両方使える)
- **タッチジェスチャー**: ゴンドラ視点のルック操作、およびエリア選択地図・離陸地点選択地図・住所検索確認地図の
  ドラッグパン+タップ選択+2本指ピンチズームを、共通ヘルパー `enablePointerNav()` でポインタイベント実装
  (マウスとタッチで同じコードパスを通る)。三人称視点のカメラ操作はOrbitControls標準のタッチ対応をそのまま利用
- **モバイルレイアウト**(`max-width: 640px` のメディアクエリ): 計器パネル縮小、住所検索欄を画面下部に再配置、
  コンパス・ヘルプ・出典パネルを縮小・再配置してタッチボタンと重ならないようにした。
  `#app` に `touch-action: none` を設定し、ブラウザ既定のスクロール/ピンチズーム/プルリフレッシュを抑止
- **未検証**: 実機スマホでの表示・操作確認はまだ行っていない。特にパネルの重なり具合(コンパス/ヘルプ/住所検索欄など)は
  実機で見ながらの微調整が必要になる見込み

## 決定事項

- **ゲーム内容**: 実在の日本の地形を3D化した空間で熱気球を操縦するフライトゲーム
- **対象エリア**: ゲーム開始前にプレイヤーが地域を選択できる(日本全国から自由に選択)
  - → 地理院タイルを動的に取得する仕組みが必要
- **ゲームの目的**: 熱気球競技の競技規定に基づく **JDG(Judge Declared Goal)競技** を行う
  - 競技委員(ゲーム側)が設定したゴール(ターゲット)にマーカーを投下し、ターゲット中心からの距離を競う
- **風のモデル**: 高度ごとに風向・風速を設定するゲーム的レイヤーモデル
  - 低〜中高度: **パイバル(パイロットバルーン)観測データを入力**して風向・風速を定義する
  - 高高度: 天気図(気圧配置)の影響を受けた風として設定
- **リアリティの方向性**: 中間(操作は簡単、浮力の慣性や着陸・投下判定はそれなりにリアル)
- **競技タスクの範囲**: まずJDG単体で完成させる。ただしタスク定義はデータ化しておき、将来PDG・FON・HWZ等を追加しやすい設計にする
- **離陸地点**: タスク設定で「個別離陸(プレイヤーがエリア内で自由に選択)」「共通ローンチサイト固定」の両方を指定できるようにする(実競技に近い形)
- **マーカー投下**: 物理落下を簡易再現。重力+空気抵抗で落下し、落下中も風に流される(低高度から投げるほど正確、という実競技の駆け引きを再現)
- **パイバルデータ入力**: 高度ごとの風向・風速をテーブルで手入力できる+典型的な風パターンのプリセットも用意
- **入力単位**: 実競技慣行に合わせる(高度はフィート刻み〔例: 500ft〕、風向は磁方位度数、風速はノット)。表示単位は設定でm/s系に切替可
- **スコアリング**: マーカー着地点からターゲット中心までの距離(m)をそのままリザルトとする。マーカー未投下時は着陸地点で計測
- **飛行中の計器・情報表示**: 以下をすべて表示する
  - 基本計器(高度計・昇降計〔バリオメーター〕)
  - 地理院地図+自機位置・軌跡・ターゲット表示(GPS相当)
  - 現在高度の風向・風速
  - ゴール(ターゲット)の位置
  - 入力済みパイバル表の参照
- **フライト前フェーズ**: 実競技のブリーフィングを再現する
  - タスクシート提示(ターゲット座標・離陸条件・制限時間)→ パイバルデータ確認/入力 → 地図で離陸地点選択 → 離陸
- **時間の扱い**: 基本は実時間スケール+プレイヤーが任意に×2〜×8の時間加速を使える(風読みのリアリティを保ちつつ1プレイを短縮)
- **視点**: ゴンドラからの一人称視点が基本、俯瞰確認用に三人称・真下視点へ切替可
- **競技相手**: まずはソロのスコアアタック。同一タスク・同一風での自己ベストや過去記録との比較機能を持たせる
- **制限時間・終了条件**: タスクシートで制限時間(スコアリングピリオド)を指定。時間内にマーカー投下できなければ着陸地点計測にフォールバック
- **操作方式**: いずれも長押し操作
  - **バーナー**: 長押しで噴射(加熱の効きは数秒遅れて現れる)
  - **排気**: リップラインを長押しで引き、気球頂部のパラシュート(リップパネル)が開いて排気する方式(ベント方式ではない)
- **燃料**: プロパン残量表示あり。消費はバーナー噴射時間に比例するシンプルなモデル(残量が実質の飛行可能時間になる)
- **記録保存・共有**: 自己ベストはブラウザのlocalStorageに保存。風+タスクの条件はURLパラメータ(シード)にエンコードし、URL共有だけで同条件に挑戦できる(サーバー不要)
- **対象エリアの広さ**: 1タスクあたり15〜20km四方(実競技の飛行エリアに近いスケール。DEM10B+必要に応じ高精度化)
- **プラットフォーム**: ブラウザ+Three.js に決定
  - 地理院タイルの動的取得と最も相性が良い(CORS対応済み・fetchでそのまま取得可)
  - 「URLを開くだけで同条件(風+タスク)に挑める」配布・共有性がソロ+記録比較の設計と噛み合う
  - 将来Electronでデスクトップ版に包む移行も容易

## 使用予定の国土地理院データ(整理済み)

- **標高タイル(DEM)**: PNG標高タイル。DEM5A(5mメッシュ・高精度)/ DEM10B(10mメッシュ・全国カバー)。地形メッシュ生成の核
- **地理院タイル(画像)**: 標準地図・淡色地図・航空写真・陰影起伏図など。地形テクスチャに使用
- **ベクトルタイル**: 道路・河川・建物輪郭など(必要に応じて)
- **Address Search API(住所検索)**: 住所・地名からの緯度経度検索に使用(2026-07-17実装)。
  番地・建物名レベルの精度は保証されないため、地図での目視確認・微調整とセットで運用
- 利用規約: 出典明記(「国土地理院タイル」等)で商用含め利用可

## 熱気球のゲーム性(前提の共有)

気球は「バーナーで上昇・冷却/排気で下降、水平移動は風任せ」という乗り物。
→ **高度によって風向・風速が変わる中を、上下操作だけで目的地を目指す**というゲーム性が自然に成立する。

## 未決定事項・次にやること(2026-07-17更新)

技術詳細設計フェーズは大筋完了し、実装が先行して進んでいる。残っているのは主に以下:

1. **実機スマホでの動作確認**(最優先): タッチボタン・ジェスチャー・レスポンシブレイアウトは
   コード実装済みだが、実機での見た目・操作感の確認・微調整は未了
   (コンパス/ヘルプ/住所検索欄など、下部パネル同士の重なり具合が特に要確認)
- 三人称視点でのゴンドラ外観、`?a=`直行時のブリーフィング表示も実機確認待ち(上記参照)
2. **共通ローンチサイト指定のタスク**: 現状は「個別離陸(自由選択)」のみ実装。
   タスク設定で離陸地点を固定する「共通ローンチサイト」方式は未実装
3. **PDG・FON・HWZ・FIN等の追加タスク種別**: JDG単体は完成。他競技種目への拡張は未着手
   (タスク定義のデータ化自体は当初方針通り意識して実装している)。
   特に**FIN(Fly In)**は、下記「地上風の日周期変動」の検討で出てきた「夕方フライトは
   1タスクのみ・キャンセルも多い」という運用実態と相性がよいタスク種別として、
   優先度高めで検討したい
4. **表示単位の切替**: 入力(ft/kt)は実装済みだが、m/s系への表示切替設定はまだ未実装

## 検討中(未着手): 地上風の揺らぎ(気圧配置モデル)— GameTargetリポジトリで検討開始(2026-07-21)

**課題**: 現状の風モデルはパイバル(高度別の風向・風速テーブル)を離陸時に固定して使うため、
30分のフライト中ずっと同じ風のまま。実際には気圧配置は時間とともに変化し、
「離陸時に観測したパイバルが、時間が経つほど実際とズレていく」という実競技のリアリティが再現できていない。

**方向性(将来的な理想形)**: 気象庁等の実際の気象データ(API)を取得して反映したいが、
現時点では技術的・データ取得コストの面で難しい。そのため、まずは**簡易的な気圧配置シミュレーション
(シード型)**で「それらしい振る舞い」を再現する方針とする。

**将来構想(2026-07-22追加)**: 実際の気象データを安定して取り込めるようになった暁には、
プレイヤーがエリア選択・タスクシート・パイバル等を自分で組み立てるのではなく、**ゲーム側(競技委員会
役)がその日・その場所の実際の風況に応じた競技設定(タスク種別・ターゲット・時刻など)を
自動で用意してくれる**、という方向性もありうる。ただし、**PDG(Pilot Declared Goal)のように
「ゴールを宣言するのはパイロット自身」というタスクも存在するため、ブリーフィング画面自体が
不要になるわけではない**。あくまで「競技委員会が行う天気読み・タスク設定」の部分が自動化され、
「パイロット自身の判断・宣言」は引き続きプレイヤーの操作として残る、という切り分けになる見込み。
これは実際の気象データ連携が前提の遠い将来の構想であり、今回の気圧配置モデル(シード型)の
検討がその土台になる位置づけ

**設計案**:

- **入力方法**: 日本地図上でプレイヤー(ブリーフィング担当者)が「高気圧(H)」「低気圧(L)」の
  位置をクリックで指示する。**H・Lはそれぞれ複数配置できる**(2026-07-24決定。実際の天気図でも
  高気圧・低気圧はそれぞれ複数現れるため)。配置できる点数の上限は次に詰める
- **地図の表示方法: フライトエリア選択画面の地図をスクロールなしで再利用する方式に変更(2026-07-24、再改訂)**:
  当初「日本列島の簡易的な形(手描きの輪郭)を固定表示する」という方針にしていたが、
  実際に手描きの輪郭で試したところ精度に限界があったため、**フライトエリア選択画面
  (`selectArea`)で使っている実際の地理院タイルの地図を、そのまま固定表示で再利用する**方針に
  変更した。パン・ズーム・ドラッグ操作は行わせず(スクロール機能なし)、日本全体+周辺が
  収まる固定の見た目のまま、その上でクリックしてH・Lを置く。地理院タイルの読み込み自体は
  既存のマップコンポーネントのロジックを踏襲しつつ、パン・ズームの操作部分だけを省いた
  専用実装とする
- **配色: 上記の変更に伴い見直し**: 地理院タイルをそのまま使うため、「陸地=薄緑、海=薄い水色」
  という天気図的な独自配色の適用は不要になった(地理院タイルの標準的な配色をそのまま使う)
- **気圧値の入力も設計に含める(2026-07-24、当初の「将来の入力検討項目」から格上げ)**: 実際の
  天気図ではH・Lの位置に加えて気圧値(例: 高気圧1018hPa、低気圧1000hPa)が併記されており、
  気圧差(=風の強さ)の計算により直結する情報のため、**H・L の位置クリックに加えて、
  各点の気圧値(hPa)も入力できるようにする**方針とする。入力方法(数値入力欄、
  スライダー等)や既定値・範囲は次に詰める
- **参考天気図の画像読み込み機能(2026-07-24追加)**: 実際の天気図(jpg等の画像ファイル)を
  読み込んで、H・L指定マップの**隣に並べて表示できる場所**を作る。プレイヤー(ブリーフィング
  担当者)が、気象庁等の実際の天気図画像を見比べながらH・L・気圧値を入力できるようにするための
  機能。画像はローカルファイルから読み込む想定(具体的な読み込み方法・UI配置・保存の要否等の
  実装の細部は未定、方向性のみ決定)
- **風の計算**: 高気圧→低気圧への気圧傾度に対し直角方向(等圧線に沿う向き、北半球なので
  高気圧を右手に見る向き=時計回りに高気圧から吹き出し、反時計回りに低気圧へ吹き込む)に
  風向を計算し、H・L間の距離や位置関係から風速を算出する、気圧配置図の一般的な読み方をベースにしたモデル
- **既存パイバルとの関係**: パイバルは「離陸時点(t=0)の正確な観測データ」という位置づけを維持する。
  気圧配置(H・L)から計算する風は、**離陸時点ではパイバルの値と一致するようキャリブレーション**し、
  そこを起点として気圧系が時間とともに変化することで、実際の風がパイバルの値から徐々にズレていく
  ・・・という体験を狙う(「パイバルは目安、時間が経つほど当てにならなくなる」という駆け引き)
- **時間変化**: 理想は、指示したH・L点がフライト中もゆっくり移動し、風が連続的に変わっていくこと。
  ただし実装難易度が高ければ、**まずはH・L固定(静止)の版から着手してよい**、とユーザーからの合意あり。
  段階実装: ①H・L固定版 → ②H・L がゆっくり移動する版、の順で検討する
- **適用範囲**: まずは**本格モード相当の新エントリーポイント「開発途中版」**に追加する
  (下記「実装先の切り分け」を参照。`?setup=1` の既存モードは変更しない)。
  SORAの簡易フロー(住所検索経由の即フライトなど)には今回は入れない
  (SORA側は「競技を知らない人向け」のコンセプトのため、気圧配置のような専門的な入力要素を増やすと
  当初の設計意図と矛盾するため、という理解。必要になれば改めて検討)
- **今回のステータス**: 設計方針の共有のみ。コードはまだ書いていない
  (次回、H・L固定版のプロトタイプ実装から着手する想定)

**気圧値の入力方法: 表形式(既存の風エディタと同様の方式)に決定(2026-07-24)**
- 地図の下に小さな表を置き、地図上でH・Lをクリックで置くたびに1行追加される方式とする
  (種別[H/L]・緯度経度・気圧[hPa]・削除ボタン、の列を持つ表)
- 点を置いた際は既定値(H=1015hPa、L=1005hPa程度、仮値)を自動で入れておき、
  気圧の数値は既存のパイバル表と同じように数値入力欄で直接編集できるようにする
- ×ボタンで行ごと削除(=その点を取り消す)
- 採用理由: SORAは既に「風エディタ」(パイバルの表形式編集UI)を持っているため、
  同じ考え方(表形式・直接編集・×ボタンで削除)を踏襲することで操作感を統一でき、
  実装・UI設計の両面でシステムが安定しやすいため

**気圧場の減衰のかけ方: L²÷(L²+距離²)方式に決定(2026-07-24、ひとまずの数字)**
- 各H・L点からの影響度を、`影響度 = L² ÷ (L² + 距離²)` という式で計算する
  (中心(距離0)で影響度100%、距離がLのところで50%、遠くに行くほど滑らかに0%へ近づく)
- L(影響が半分になる目安距離)は**400〜600km程度**を仮値とする(これまで参照した天気図の
  等圧線の間隔・高低気圧の広がり方から見た大まかな目安。要調整)
- この式を選んだ理由: (1)急に0になる境界線がなく、場所によって風向・風速が不自然に
  ガクッと変わらない、(2)式が滑らかなので勾配(風向・風速の計算に必要)も素直に計算できる、
  (3)Lという1つの数字だけで影響の広がり方を調整できる(チューニングしやすい)

**配置数の上限: H・Lそれぞれ4〜5個程度に決定(2026-07-24、ひとまずの数字)**
- これまで参照した実際の天気図2枚を数えたところ、台風・熱帯低気圧を除くと
  H(高気圧)2〜4個・L(低気圧)3〜4個程度が同時に現れていた
  (7/23 3時: H4個・L4個 / 7/24 18時: H2個・L3個)
- サンプル2枚のみの目安であり、統計的に確定した数字ではないが、実際の天気図の複雑さを
  十分カバーできる数として、**ひとまずH・Lそれぞれ上限4〜5個**とする
- 風速の距離減衰・上限のかけ方(H・L の近くで極端に強い風にならないようにする調整)
- H・L移動時の速度・移動方向をどう決めるか(ランダムか、プレイヤー指定か)
- 既存の高度別パイバルテーブルとの合成方法(低高度は気圧配置寄り、高高度は既存パイバル寄り、
  のようなブレンドが必要か)

**実際の天気図・風予測との比較で得た知見(2026-07-21、スクリーンショット参照)**:
- 気象庁「風予測」の地上風は、等圧線に完全並行(理論上の地衡風)ではなく、
  低気圧の中心へ**やや斜めに巻き込むように吹き込む**(地表摩擦の影響)。
  「高気圧を右手に見る向き」だけの単純化ではやや不自然になりうるため、
  **低気圧側へ10〜30°程度傾ける補正**を入れると実際の見た目に近づく
- 同じ資料の上空風は、地上風に比べて広域で一様な流れになりやすい(偏西風の影響が支配的で、
  個々のH・Lの影響が弱い)。これは「H・Lモデルは地上・低高度専用とし、上空(高高度)は
  既存のパイバル入力のままでよい」という前回の方針を裏付ける結果と言える
- (2026-07-22追加)地上風と上空風(標高約1000m)を見比べると、**地上風は上空風より明確に弱い**
  (地表摩擦による減速)。実際の資料では地上は薄い水色(5m/s未満)中心、上空は緑〜黄色
  (5〜15m/s)の帯が広く見られた
- (2026-07-22追加)同一地点(小山市・佐賀市)の7/21・7/22の時間別データを比較すると、
  **夕方(19〜21時台)はまだ2〜5m/s程度の風が残り、深夜〜明け方(0〜7時台)にかけて
  0〜1m/s程度まで穏やかになる**という日周期パターンが日をまたいでも再現されていた。
  熱気球は有視界飛行(VFR)で日没後は飛行不可のため、「夕方」フライトは必然的にこの
  「まだ穏やかになりきっていない時間帯」に限られる、という制約につながった(下記モデル参照)

**H・L複数配置への対応: 気圧場(スカラー場)の勾配方式に決定(2026-07-24)**

これまでの設計は「H・L 各1点ずつ」を前提にしていたが、実際の天気図のように**H・L を複数配置
できるようにする**方針に変更。これに伴い、風の計算方法も以下のように一般化する
(H・Lが1個ずつのケースは、この方式の特別な場合として自然に含まれる):

1. **気圧場を作る**: 地図上の任意の地点の気圧を、配置された全てのH・L点からの影響を
   足し合わせて計算する。各点は「標準気圧(1013hPa)からのズレ」を中心に持ち、
   中心から離れるほど影響が弱まる(距離による減衰。具体的な減衰のかけ方・影響が届く
   目安距離は**要調整の仮値**、次に詰める)
2. **勾配から上空相当の風(傾度風)を出す**: 任意の地点における気圧場の傾き(勾配、
   =どの向きにどれだけ急に気圧が変化しているか)を計算し、勾配に直角な向き(等圧線に沿う向き、
   北半球なので低気圧を左手に見る向き=反時計回りに低気圧へ、時計回りに高気圧から吹き出す)に
   風向を、勾配の急さに応じて風速を決める
3. **地上風 = 上空の風 × 減速係数**(目安6〜7割、要調整の仮値)、かつ低気圧側(=勾配が
   下る方向)へ角度を振る(10〜30°程度の補正、実際の天気図の吹き込み角度から)
4. さらに**日周期係数**(早朝・夕方は穏やか)を掛けて、最終的な地上風とする

これにより、これまでの「H・L 1点ずつの単純な位置関係」という設計は、この一般化された
気圧場方式の**特別な場合(点が2つだけの場合)**として自然に含まれる形になった

**旧・風計算モデルの整理(3段階、2026-07-22時点、参考として残す)**:
1. H・Lの位置関係から**上空相当の風(傾度風)**を計算する(等圧線に沿う向き、強さは気圧差から算出)
2. **地上風 = 上空の風 × 減速係数**(目安6〜7割。実際の資料からのおおまかな比率であり、
   **要調整の仮値**)、かつ低気圧側へ角度を振る(上記の吹き込み角度の補正)
3. さらに**日周期係数**(早朝・夕方は穏やか)を掛けて、最終的な地上風とする

パイバル(離陸時の観測値)は、上記モデル全体を離陸時点でキャリブレーションする基準として使う、
という位置づけは変わらない

**ステップ3: 風の計算式の具体化(2026-07-24)**

- **気圧勾配 → 風向・風速の変換**:
  1. 任意の地点での気圧場の勾配ベクトル(どちら向きにどれだけ急に変化しているか)を計算する
  2. **風向**: 勾配ベクトル(気圧が上がる方向)を90°時計回りに回転させた向き
     (=気圧が下がる方向を時計回りに回した向き)。「高気圧の周りは時計回り、
     低気圧の周りは反時計回り」というルールと一致する
  3. **風速**: 勾配の急さ(hPa/100kmあたり)に、変換係数K(仮値)を掛けて算出。
     Kの正確さは次のキャリブレーションで吸収されるため、初期値は仮でよい
- **パイバルとの整合(キャリブレーション)**: 離陸時点(t=0)で、この計算式から出る「地上風」を
  計算し、**パイバル表の一番低い高度(0ft)の値**と比較する。両者の差(倍率・角度のズレ)を
  「補正係数」として記録し、以後もその補正係数をかけ続ける。これにより、Kや減衰距離Lの正確さを
  気にしなくても、離陸時点は必ずパイバルと一致し、そこから自然にズレていく、という設計意図が保たれる
- **「地上風」に厚みを持たせる(2026-07-24、修正)**: 「地上風」は0ftだけの値ではなく、
  地表付近にある程度の厚みを持った層として扱う。**「地上層」(仮に0〜1000ft程度、要調整の仮値)を
  設定し、層の内部では地上(0ft、気圧配置モデルの値)から層の上端(1000ft、パイバル表の値)へ
  滑らかに移行(ブレンド)**する。層の外(1000ft超)は今まで通りパイバル入力のみで変更なし。
  これにより、0ftと1000ftの間で風が不自然にガクッと変わることを避ける
- **計算過程の表示(2026-07-24追加)**: モデルはまだ現実との乖離がありうる段階のため、
  最終的な風の値だけでなく、**計算の途中経過(生の勾配風、減速・角度補正後の値、
  キャリブレーション補正係数、ブレンド後の最終値など)を画面に表示**できるようにする。
  開発中はモデルの妥当性確認・調整に使え、将来的には遊ぶ人への透明性(なぜこの風になったか
  が分かる)にもつながる可能性がある。表示場所・UIの形式は次に詰める
- **要調整の仮値は、表示しつつ入力して修正も可能にする(2026-07-24決定)**: これまで
  「要調整の仮値」としてきた以下の数値は、コードに固定で埋め込むのではなく、
  開発途中版の画面上に**表示した上で、その場で数値を入力・修正できる**ようにする方針とする
  (計算過程の表示と合わせて、モデルの調整をコード変更なしで試行錯誤できるようにする狙い):
  - K(気圧勾配→風速の変換係数)
  - L(気圧場の減衰距離、目安400〜600km)
  - 減速係数(上空風→地上風、目安6〜7割)
  - 角度補正(低気圧側への振れ、目安10〜30°)
  - 地上層の厚み(目安0〜1000ft)
  (配置数の上限[H・Lそれぞれ4〜5個]は仕様上の制約であり、この調整可能パラメータの対象外)

## 検討中(未着手): 地上風の日周期変動 — GameTargetリポジトリで検討開始(2026-07-21)

**きっかけ**: 実際の地点別天気予報(時間毎)を見ると、**日中は風が強く、夜間〜早朝にかけて
弱まって0m/s近くまで落ち着く**という日周期パターンが明確に見られた(気圧配置によるH・Lの
広域風とは別レイヤーの変動)。実競技で熱気球フライトが早朝・夕方に行われることが多いのも、
この「朝夕は風が穏やかになる」性質を利用しているため。この日周期変動もモデルに含めることにした

**設計案**:
- 気圧配置(H・L)モデルによる広域の風向・風速に対し、**時刻による強弱の係数**を掛け合わせる形で
  日周期変動を表現する(風向にはあまり影響させず、主に風速の強弱に反映するイメージ)
- **地上風5m/s前後は実競技でもフライトキャンセルの目安**という実務知識を踏まえ、
  「フライト開始時刻」の選択肢は**早朝・夕方のみ**とする(実際の競技と同様、日中はそもそも
  選べない)。日中(昼)は地上風が強くなりやすく本来飛べない時間帯、という位置づけのため、
  選択肢に含めない
- **早朝と夕方は対称ではない**: 熱気球は有視界飛行(VFR)のため、**日没後は飛行できない**という
  制約がある。そのため「夕方」は必然的に日没前(まだ明るい時間帯)に限られ、実際の気象データを
  見ても夕方(19〜21時台)は地上風がまだ2〜5m/s程度残っていることが多く、**穏やかになりきる前の
  時間帯で飛ぶことになりやすい**。一方「早朝」は日の出前後で、実データでも0〜1m/s程度まで
  下がった後の時間帯を選べるため、より安定して穏やか
  - → **早朝: 比較的安定して穏やか(キャンセルリスク低)**
  - → **夕方: 風がまだ残っていることがあり、キャンセルライン(5m/s前後)に近い/超えることもある
    (キャンセルリスクやや高、より挑戦的な選択肢)**という非対称なカーブ・扱いにする
- **競技運営との対応(2026-07-22追加、実務知見)**: 現実の競技でも夕方フライトはキャンセルが多く、
  実施できても**タスクを1つだけ(FIN=Fly Inが多い)**にすることが多い。一方、早朝は安定していれば
  **複数タスクの競技**を行う。この構造をゲームにも反映できると、時刻選択がタスク構成にも
  意味を持つようになる:
  - **早朝を選ぶ**→ 安定していれば複数タスク(JDGを複数回、あるいは将来追加するPDG等との
    組み合わせ)に挑戦できる
  - **夕方を選ぶ**→ 基本的に1タスクのみ。キャンセルの可能性も織り込む。タスク種別としては
    **FIN(Fly In)**との相性がよさそう(下記「未着手」項目のタスク拡張とも関連)
- **フライトの時刻設定**: 現状のSORAには時刻(朝・昼・夜)の概念がなく、太陽は固定位置の
  ディレクショナルライトで昼夜サイクルは実装されていない。今回はビジュアル面(空の見た目・光)は
  変更せず、**ブリーフィングで「フライト開始時刻」(早朝 / 夕方の2択)を選択できるようにし、
  その値を風の強弱計算だけに反映する**という方針にした(見た目のフル昼夜サイクル実装は範囲外)
- 適用範囲は気圧配置モデル・地上クルー機能と同様、新エントリーポイント「開発途中版」のみ
  (下記「実装先の切り分け」を参照)
- **今回のステータス**: 設計方針の共有のみ。コードはまだ書いていない

**未決定の細部(次に詰めること)**:
- 「フライト開始時刻」の入力UI(早朝・夕方の2択プルダウン/ボタンなど)
- 時刻→風速係数の具体的なカーブ(早朝は低め安定、夕方は実データ(19〜21時台で2〜5m/s程度)を
  参考にしたやや高め・ばらつきありのカーブ、というイメージだが数値は未設計)
- キャンセル判定の要否(夕方選択時、計算結果が5m/s前後を超えた場合に「フライトキャンセル」
  相当の扱いにするか、単に難易度が上がるだけにするか)
- 早朝(複数タスク)・夕方(1タスク、FIN想定)というタスク数・タスク種別の出し分けを
  ブリーフィング画面にどう反映するか(FIN自体は上記「未着手」項目でまだ未実装のため、
  当面は夕方も暫定的にJDG1本のみ、という扱いになる見込み)
- ゲーム内時間加速(×1〜×8)との関係(フライト中に時刻が進んで日周期の位相も動くのか、
  それとも開始時刻の係数で固定するのか)

## 検討中(未着手): 地上クルー機能 — GameTargetリポジトリで検討開始(2026-07-21)

**位置づけ**: 上記「地上風の揺らぎ(気圧配置モデル)」と連動する追加機能。
パイバルは離陸時点(t=0)の情報のまま固定なので、時間が経つほど実際の風とのズレが広がっていく
(気圧配置モデルの設計意図)。この「今の本当の風」を知る手段として、地上クルーへの無線問い合わせを
用意する

**設計案**:

- **操作**: プレイヤーがボタンを押したとき(問い合わせ形式)だけ、地上クルーが応答する。
  常時表示や一定間隔の自動連絡ではない
- **報告内容**: **ターゲット付近の地上風**(風向・風速)を、問い合わせた時点の気圧配置モデルの
  計算値から報告する。気球の現在位置の真下ではなく、あくまでターゲット周辺固定
- **ゲーム性**: 離陸時のパイバル(過去の情報)と、地上クルーからの最新報告(現在の情報)を
  照らし合わせて、マーカー投下のタイミング・高度戦略を判断する、という駆け引きを狙う
- **適用範囲**: 気圧配置モデルと同様、新エントリーポイント「開発途中版」のみ
  (下記「実装先の切り分け」を参照)
- **今回のステータス**: 設計方針の共有のみ。コードはまだ書いていない

**未決定の細部(次に詰めること)**:
- ボタンの配置・キー割当(既存のP/M等と衝突しない割当が必要)
- 問い合わせ回数に制限を設けるか(無制限だと駆け引きが薄れる可能性。回数制限 or クールダウンの要否)
- 応答の見せ方(即時表示か、無線交信らしい遅延・演出を入れるか)
- 地上クルー機能の実装は、気圧配置モデル(H・L)の実装が前提となるため、着手順序は
  「気圧配置モデル → 地上クルー」の順になる見込み

## 長期ビジョン: 「風を予測できるシステム」に向けて足りないもの(2026-07-23)

**問い**: 将来的には「それらしく見えるシミュレーション」を超えて、**風を予測できるシステム**に
なりたい、という長期的な理想が共有された。現状とのギャップを4層に整理する。

**開発の姿勢**: 開発はこの理想に近づくために段階的に進んでいく、という方針を確認。
今すぐ全部を実現するものではなく、今回までに検討した気圧配置モデル・地上クルー・日周期変動・
地形風などは、いずれもこの理想への「途中の足場」として位置づけられる

**現状とのギャップ(4層)**:

1. **本物の気象データそのもの**: 現状はプレイヤーがH・L2点を指定する「シード型」の代用品。
   本当に予測するには、気象庁のGPV(数値予報格子点)データやアメダス実況等をAPIで取り込む
   仕組みが必要(「地上風の揺らぎ」節の「方向性(将来的な理想形)」と対応、現状は未着手)
2. **予測の"物理"そのもの**: 現状の設計(等圧線に沿う・低気圧側へ角度を振る・地表摩擦6〜7割等)は
   あくまで「それらしく見える近似」であり、予測ではない。本当に予測するには、時間発展する
   数値モデル(気圧配置の動きそのものを計算で予測する)か、過去データから学習する統計・
   機械学習モデルのどちらかが必要。現状はどちらもなく、「今の状態を模倣する」ことしかできていない
3. **答え合わせの仕組み(検証データ)**: モデルが当たっているかを確認する手段が現状ない。
   前述の「フライトログの寄付」案・「実測観測データとの突き合わせ」案が、この役割を担う候補。
   予測システムは「予測 → 答え合わせ → 調整」のループがないと精度が上がっていかない
4. **サーバー・インフラ**: 気象データの取得も、フライトログの収集も、学習モデルの運用も、
   すべて現状の「サーバー不要」という設計方針とぶつかる。技術的な難しさというより、
   **設計思想の転換をいつ・どこまでやるか**という意思決定の問題として捉えている

**現時点の整理**: 今できているのは「それらしく見えるゲーム的なシミュレーション」であり、
「予測」と呼べるようになるには、上記4層(データ・物理/AI・検証・インフラ)が今後段階的に
必要になる、という認識で一致

## 将来の入力検討項目: 風予測精度の向上候補(2026-07-23)

天気図(気圧配置)以外に、将来的に風の予測精度を上げるために入力・考慮したい候補。
優先度が高そうな順に列挙(いずれも未設計・未着手、方向性のみの記録)。

1. **海陸分布(海風・陸風)**: 日本は海に囲まれているため、気圧配置とは別に、日中は海→陸
   (海風)、夜間は陸→海(陸風)という局地風が強く効く。SORAは既に地形データで海と陸を
   区別している(海上など標高タイル欠損は標高0として処理)ため、この情報がそのまま
   海風・陸風の判定に使える可能性がある。「地上風の日周期変動」モデルとも相性がよく、
   自然に統合できそうな候補として優先度高め
2. ~~**気圧差の実数値(等圧線の間隔)**~~ → **2026-07-24、現在の設計に格上げ済み**(「地上風の揺らぎ」
   節を参照。H・Lの位置に加えて各点の気圧値(hPa)も入力する方針に決定したため、この項目は
   もう「将来の候補」ではなく現行の設計の一部になった
3. **天気(晴れ/曇り/雨)**: 日射の強さが地表の温まり方に影響するため、日周期変動モデルの
   「昼にどれだけ強まるか」の精度向上に使える可能性がある。実データでも、終日晴れの地点は
   風が着実に強まる一方、途中で曇り・雨に変わる地点はやや風が落ち着く傾向が見られた
4. **実測観測データ(アメダス等)を参照点として使う**: ゲーム内に直接組み込むというより、
   モデルの妥当性を検証・調整するための基準データとして活用する案。上記の「フライトログ寄付」
   案とも近い発想(実測とモデル出力を突き合わせて精度を上げる)

## 将来検討(方向性のみ・未着手): 前線・台風(2026-07-24)

実際の天気図(気象庁)には、H・L(高気圧・低気圧)以外にも**前線**(温暖前線・寒冷前線などを表す線)や
**台風**(実況では「台12号 990hPa」のように表示)が描かれている。今回のH・L気圧配置モデルの検討
過程で見つかった要素で、方向性の検討はまだしていない

- **前線**: 前線付近は天候・風が急変しやすい領域として知られる。H・Lモデルに加えるとすれば、
  前線に沿った局地的な風の乱れ、のような形が考えられるが未検討
- **台風**: 極端に強い低気圧(990hPa等、通常のLよりかなり低い)として捉えられる。既存の
  L(低気圧)モデルの延長として、より強い気圧差のケースとして扱える可能性があるが、
  暴風域・雨域などの追加要素も伴うため、単純な延長でいいかは要検討

いずれも今回のH・L気圧配置モデル(ステップ1〜3)の優先度のほうが高く、その先の将来検討事項として扱う

## 将来検討(方向性のみ・未着手): 地形風(河川風)— GameTargetリポジトリで言及(2026-07-22)

**内容**: 地上風には、気圧配置(H・L)による広域の流れとは別に、**地形の影響を受けた局地風
(地形風)**もある。特に川沿い・谷筋では、風が地形に沿って(川の流れに沿うように)吹きやすい、
という現実の気象現象(谷風・チャネリング効果)がある。将来的にはこれにも対応したい、という
方向性のみ言及があった

**位置づけ**: 今回設計している気圧配置モデル(広域)・日周期変動(時間帯)・地上クルー(報告手段)
とは別の、**第4のレイヤー(局地的な地形の影響)**として捉えられる。SORAは実際のDEM地形データを
エリアごとに読み込んでいるため、将来的には地形データ(谷・河川の位置)から自動的に計算する
可能性も考えられるが、今回時点では**計算方法も含めてまだ漠然としており、方向性の記録のみ**
(具体的な設計は行っていない)

**今回のステータス**: 「将来的には対応したい」という方向性の共有のみ。設計・実装ともに未着手。
上記3機能(気圧配置モデル・日周期変動・地上クルー)の優先度のほうが高く、地形風はその先の
将来検討事項として扱う

**データソースの案(2026-07-22追加)**: 地形風の裏付けとして、**実際に気球で飛んだフライトログを
集める**というアイデアが出た。実際にどう風に流されたかの軌跡データが集まれば、地形風モデルの
検証・調整に活用できる可能性がある(気圧配置モデル側の検証にも使えるかもしれない)。
ログの収集方法・フォーマット(熱気球・グライダー界隈で使われる IGC ファイル形式など)・
収集の仕組み(コミュニティ連携、アプリ経由での収集等)は現時点では未検討。
良い方法が見つかれば都度提案する

**追加案: SORAのプレイログを寄付してもらう仕組み(2026-07-22)**: 実際の気球のフライトログとは別に、
**SORAで遊んだプレイヤー自身のフライトログ(ゲーム内の位置・高度・風の記録)を寄付してもらう**、
という案も出た
- 技術的には、ゲーム内で毎フレーム計算している位置・高度・風向風速をそのまま記録すればよく、
  記録自体のハードルは低い
- ただし**「集める」ためには、ログを受け取って保存するサーバー(バックエンド)が新たに必要**になる。
  SORAはこれまで「サーバー不要」(URLパラメータ+ブラウザのlocalStorageのみで完結し、
  共有もURLだけで完結)という方針で一貫して作ってきたため、これは設計思想の大きな転換点になる
- **今回のステータス**: サーバー導入の是非も含め判断は先送りし、方向性のみメモに記録。
  次に検討する際は、サーバーを導入してでも実現するか、あるいはサーバーなしでできる代替案
  (例: プレイヤーがURLやファイルで手動提出する形式など)を探るか、という分岐から検討を始める見込み

## 実装先の切り分け: 「開発途中版」エントリーポイントの新設(2026-07-22)

**変更点**: 上記3機能(気圧配置モデル・地上風の日周期変動・地上クルー)は、当初
「本格モード(`?setup=1`)に追加する」としていたが、方針を変更し、**新しく「開発途中版」という
別のエントリーポイントを設けて、そちらに実装する**ことにした

**理由**:
- `?setup=1` は、これまで「本格モード」オーバーレイの「お試しでやってみる(最新版ではありません)」
  ボタンから遷移する、**hot-air-balloon1相当の安定版フルJDGブリーフィング**として運用してきた
  (仕様・挙動を変えていない、動作が保証された基準点)
- ここに実験段階の新機能(気圧配置モデル等)を直接組み込むと、この「安定した基準点」としての
  性質が損なわれる。新機能はまだ設計段階で細部も未確定なものが多いため、
  **既存の`?setup=1`とは別に切り離して置く**ほうが安全
- 「開発途中版」であることが分かる形にすることで、プレイヤー側にも「ここは開発中の実験的な機能」
  という前提が伝わりやすくなる

**今回のステータス**: 仕様検討メモへの設計変更の記録のみ。実際のUI(ボタン追加等)への実装は
まだ行っていない。次回、必要になったタイミングでコーディングに着手する

**中身の範囲: フルJDGベースに決定(2026-07-22)**
- 「開発途中版」の中身は、フルJDGブリーフィング(`?setup=1`相当: エリア選択・タスクシート・
  パイバル編集・地図での離陸地点選択)をベースにすることに決定
- 理由: 気圧配置モデルは「離陸時点でパイバルとキャリブレーションし、時間とともにズレていく」
  設計のため、パイバル入力・離陸地点選択という文脈がないと成立しない。地上クルー機能も
  「離陸時のパイバル(過去)」と「地上クルー報告(現在)」を比較する駆け引きのため、同様に
  フルフローが前提。単独の実験画面にすると、何と何を比較しているのかが伝わらなくなる
- 既存のエリア選択マップ・離陸地点選択マップ等のUIコンポーネントは、H・L指定にもほぼ
  そのまま流用できる見込み(`setupAreaMap`のH・L選択用への転用など)

**実装方針: 「開発途中版」は独立したモードとして切り分ける(2026-07-22)**
- `setupMode`の分岐に新機能を直接継ぎ足すのではなく、**「開発途中版」を独立したモード
  (例: `devMode`)として切り分け、既存のsetupModeのコードをコピーする形で実装を始める**方針に決定
- 理由: main.jsは1ファイルに`setupMode`の分岐が随所に散らばっているため、既存コードに
  直接拡張を続けると、新機能側のバグと既存機能側のバグの切り分けが難しくなり、
  安定版(`?setup=1`)を壊すリスクが上がる。多少コードの重複は生まれるが、
  「開発途中版」という位置づけ上、安定版から独立させておくほうが安全という判断
- これにより `?setup=1`(安定版)と `?dev=1`(開発途中版)は、
  当面はコードレベルでも別系統として管理される見込み

**未決定の細部(次に詰めること)**: なし。実装先の切り分けに関する主要な論点はすべて決定済み
(次回はコーディングに着手できる状態)

**入り口の配置: 「お試しでやってみる」ボタンの下に決定(2026-07-22)**
- 「開発途中版」への入り口は、「本格モード」オーバーレイ内の**「お試しでやってみる
  (最新版ではありません)」ボタンのすぐ下**に新しいボタンを追加する形にする
- 同じオーバーレイ内に、hot-air-balloon1相当の安定版(お試しでやってみる → `?setup=1`)と
  新機能の実験版(開発途中版 → `?dev=1`)が並ぶことで、両者の位置づけの違い
  (安定版か実験版か)が視覚的にも伝わりやすい配置

**コード複製の範囲: できるだけ大きく複製する方針に決定(2026-07-22)**
- `devMode`用のコードは、既存の`setupMode`と条件分岐で共有する範囲を最小限にし、
  **できるだけ大きく複製する**方針とする(HTML側のブリーフィング関連要素、JS側の関数群とも、
  極力`devMode`専用のコピーを作る)
- 理由: 「安定版(`?setup=1`)を壊すリスクを避ける」ことを最優先するため。共有部分が多いほど、
  `devMode`側の変更が`setupMode`側に意図せず影響する可能性が高まる。多少のコード重複・保守コストは
  許容し、安定版の独立性を優先する
- この方針により、次回コーディング時は「既存の`setupMode`関連コードをコピーして`devMode`用の
  新しい関数・要素群を作り、そこに新機能を追加していく」進め方になる見込み

**URLパラメータ名: `?dev=1` に決定(2026-07-22)**
- 開発途中版へのアクセスは `?dev=1` を使う(既存の `?setup=1` に倣った命名)
- `?a=lon,lat` や将来の風パラメータ等と併用できる想定(例: `?dev=1&a=lon,lat`)

## 今後の検討項目・全体見取り図

本プロジェクトには別途「次回検討項目」ファイルあり(非公開)。
